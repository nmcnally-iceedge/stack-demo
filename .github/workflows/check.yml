name: check
on:
  pull_request:
    branches:
      - develop
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: test -f README.md && echo "structure ok"
